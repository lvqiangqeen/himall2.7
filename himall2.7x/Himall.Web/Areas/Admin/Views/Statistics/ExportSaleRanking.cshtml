@model  List<Himall.Model.SalesExportModel>
@{
    object Title = new object();
    ViewData.TryGetValue("Title", out Title);
}
<table cellspacing="0" cellpadding="5" rules="all" border="1">
    <thead>
        <tr>
            <th colspan="3">@(ViewData["Title"].ToString())</th>
        </tr>
        <tr>
            <th>商品</th>
            <th>销售量</th>
            <th>销售额</th>
        </tr>
    </thead>
    <tbody id="list-table-body">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ProductName</td>
                <td>@item.SaleCount</td>
                <td>@item.SaleAmount.ToString("F2")</td>
            </tr>
        }
    </tbody>
</table>